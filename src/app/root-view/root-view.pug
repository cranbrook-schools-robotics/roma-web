h1 Roma
md-toolbar
  | All Organizations
md-content
  md-list(flex)
    md-list-item(
      ng-repeat='org in $ctrl.organizations'
      ui-sref='organization({organizationURI: org.uri})'
    ) {{ org.name }}
