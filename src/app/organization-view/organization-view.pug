h1(ng-if='!$ctrl.organization') Could not find that organization

h1 {{ $ctrl.organization.name }}
div(layout='row' layout-fill layout-margin layout-padding ng-cloak)
  section(ng-repeat='division in $ctrl.organization.divisions')
    md-button(ui-sref='division({divisionURI: division.uri})')
    division-view(division='division')

